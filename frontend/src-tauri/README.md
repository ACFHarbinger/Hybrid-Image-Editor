# Tauri host boundary

This directory reserves the native Tauri host for HIE’s standalone desktop mode. The web UI in `../src/` is also designed to be mounted as a tab in Image-Toolkit’s Tauri application. The Rust host configuration and command bridge will be added when the middleware IPC contract is finalized.
