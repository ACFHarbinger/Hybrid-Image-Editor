# tools/bench — run benchmarks for C++.

set shell := ["bash", "-c"]

all: cpp

cpp:
    cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DBUILD_BENCHMARK=ON && cmake --build build --target hybrid_image_editor_benchmark -j && ./build/logic/benchmark/hybrid_image_editor_benchmark
