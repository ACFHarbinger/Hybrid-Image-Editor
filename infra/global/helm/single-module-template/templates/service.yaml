apiVersion: v1
kind: Service
metadata:
  name: {{ include "single-module-template.name" . }}
  labels:
    {{- include "single-module-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "single-module-template.labels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
