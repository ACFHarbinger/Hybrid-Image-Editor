apiVersion: v1
kind: Service
metadata:
  name: hybrid-image-editor
  labels:
    app: hybrid-image-editor
spec:
  selector:
    app: hybrid-image-editor
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
